===== Revamp stage 2.md =====

<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" class="logo" width="120"/>

# Read all added sources.

I have revamped my portfolio website like you suggested, the sources are attached.
suggest next stage of changes/updates/ideas you have and, give packages names and cmds to install, and give codes for each page.

Suggest brilliant ideas to make this website even better and attractive that leaves user in awe, so i can land clients more often.

dir structure:

```markdown
. 📂 portfolio-website
├── 📄 README.md
└── 📂 [project]/
│  └── 📂 logger.js [app-rsc] (ecmascript)/
│    └── 📂 logs/
│      ├── 📄 info.log
│  └── 📂 logger.js [app-ssr] (ecmascript)/
│    └── 📂 logs/
├── 📄 eslint.config.mjs
├── 📄 logger.js
└── 📂 logs/
│  ├── 📄 info.log
├── 📄 next.config.ts
├── 📄 package-lock.json
├── 📄 package.json
├── 📄 postcss.config.mjs
└── 📂 public/
│  ├── 📄 file.svg
│  ├── 📄 globe.svg
│  └── 📂 icons/
│    ├── 📄 contact_discord.svg
│    ├── 📄 contacts_title_line.svg
│    ├── 📄 discord_contact.svg
│    ├── 📄 discord_footer.svg
│    ├── 📄 dribble_social.svg
│    ├── 📄 email_contact.svg
│    └── 📂 figma/
│      ├── 📄 burger-icon.svg
│      ├── 📄 burger-open.svg
│      ├── 📄 burger.svg
│      ├── 📄 close-icon.svg
│      ├── 📄 dribble.svg
│      ├── 📄 figma.svg
│      ├── 📄 github.svg
│      ├── 📄 logo-default.svg
│      ├── 📄 logo-outline.svg
│      ├── 📄 social-dribble.svg
│      ├── 📄 social-figma.svg
│      ├── 📄 social-github.svg
│    ├── 📄 figma_footer.svg
│    ├── 📄 figma_social.svg
│    ├── 📄 footer_discord.svg
│    ├── 📄 footer_figma.svg
│    ├── 📄 footer_github.svg
│    ├── 📄 github_footer.svg
│    ├── 📄 github_social.svg
│    ├── 📄 logo-outline.svg
│    ├── 📄 logo.svg
│    ├── 📄 logo_header.svg
│    ├── 📄 quote_end.svg
│    ├── 📄 quote_mark_left.svg
│    ├── 📄 quote_start.svg
│  └── 📂 images/
│    ├── 📄 about_me_profile.png
│    ├── 📄 about_me_title_line.svg
│    └── 📂 figma/
│      ├── 📄 burger-image.png
│    ├── 📄 hero-image.png
│    ├── 📄 hero_image.png
│    ├── 📄 project_chertnodes.png
│    ├── 📄 project_kahoot_viewer.png
│    ├── 📄 project_protectx.png
│    ├── 📄 projects_title_line.svg
│    ├── 📄 skills_title_line.svg
│  ├── 📄 next.svg
│  ├── 📄 vercel.svg
│  ├── 📄 window.svg
└── 📂 src/
│  └── 📂 app/
│    └── 📂 (components)/
│      ├── 📄 Dots.tsx
│      ├── 📄 Footer.tsx
│      ├── 📄 Header.tsx
│      ├── 📄 LineDeco.tsx
│    └── 📂 (sections)/
│      └── 📂 home/
│        ├── 📄 AboutMeSection.tsx
│        ├── 📄 ContactSection.tsx
│        ├── 📄 HeroSection.tsx
│        ├── 📄 ProjectsSection.tsx
│        ├── 📄 QuoteSection.tsx
│        ├── 📄 SkillsSection.tsx
│    └── 📂 about/
│      ├── 📄 page.tsx
│    └── 📂 contact/
│      ├── 📄 page.tsx
│    ├── 📄 favicon.ico
│    ├── 📄 globals.css
│    ├── 📄 layout.tsx
│    ├── 📄 page.tsx
│    └── 📂 projects/
│      ├── 📄 page.tsx
│  └── 📂 components/
│    └── 📂 magicui/
│      ├── 📄 animated-gradient-text.tsx
│      ├── 📄 dot-pattern.tsx
│      ├── 📄 number-ticker.tsx
│      ├── 📄 particles.tsx
│  └── 📂 lib/
│    ├── 📄 logger.ts
└── 📄 tsconfig.json
```

tech stack:

PS D:\Personal\Portfolio\Portfolio-Website\portfolio-website> npm list
portfolio-website@0.1.0 D:\Personal\Portfolio\Portfolio-Website\portfolio-website
├── @emnapi/core@1.4.3 extraneous
├── @emnapi/runtime@1.4.3 extraneous
├── @emnapi/wasi-threads@1.0.2 extraneous
├── @eslint/eslintrc@3.3.1
├── @napi-rs/wasm-runtime@0.2.10 extraneous
├── @radix-ui/react-slot@1.2.3
├── @react-spring/web@10.0.1
├── @tailwindcss/postcss@4.1.8
├── @tybys/wasm-util@0.9.0 extraneous
├── @types/node@20.17.52
├── @types/react-dom@19.1.5
├── @types/react@19.1.6
├── class-variance-authority@0.7.1
├── clsx@2.1.1
├── eslint-config-next@15.3.2
├── eslint@9.27.0
├── framer-motion@12.23.6
├── gsap@3.13.0
├── lucide-react@0.525.0
├── motion@12.23.6
├── next@15.3.2
├── react-dom@19.1.0
├── react-intersection-observer@9.16.0
├── react@19.1.0
├── tailwind-merge@3.3.1
├── tailwind-scrollbar-hide@4.0.0
├── tailwind-scrollbar@4.0.2
├── tailwindcss-animate@1.0.7
├── tailwindcss@4.1.8
└── typescript@5.8.3

Looking at your impressive portfolio website, I can see you've built a solid foundation with modern technologies. Here are **game-changing improvements** that will make your portfolio absolutely **jaw-dropping** and help you land more clients:

## 🚀 Next-Level Enhancements

### 1. **3D Interactive Background with Three.js**

Transform your static background into an interactive 3D space.

**Install packages:**

```bash
npm install three @types/three @react-three/fiber @react-three/drei leva
npm install @react-three/postprocessing three-stdlib
```

**Create 3D Background Component:**

```tsx
// src/components/3d/InteractiveBackground.tsx
'use client'

import { Canvas, useFrame, useThree } from '@react-three/fiber'
import { useRef, useMemo } from 'react'
import { Points, PointMaterial, Sphere } from '@react-three/drei'
import * as THREE from 'three'

function AnimatedStars() {
  const ref = useRef<THREE.Points>(null)
  const sphere = useMemo(() => {
    const positions = new Float32Array(2000 * 3)
    for (let i = 0; i < 2000; i++) {
      const i3 = i * 3
      positions[i3] = (Math.random() - 0.5) * 20
      positions[i3 + 1] = (Math.random() - 0.5) * 20  
      positions[i3 + 2] = (Math.random() - 0.5) * 20
    }
    return positions
  }, [])

  useFrame((state, delta) => {
    if (ref.current) {
      ref.current.rotation.x -= delta / 10
      ref.current.rotation.y -= delta / 15
    }
  })

  return (
    <Points ref={ref} positions={sphere} stride={3} frustumCulled={false}>
      <PointMaterial
        transparent
        color="#C778DD"
        size={0.05}
        sizeAttenuation={true}
        depthWrite={false}
      />
    </Points>
  )
}

export function InteractiveBackground() {
  return (
    <div className="fixed inset-0 -z-10">
      <Canvas camera={{ position: [0, 0, 1] }}>
        <AnimatedStars />
      </Canvas>
    </div>
  )
}
```


### 2. **Advanced Cursor Interaction System**

Create a magnetic cursor that responds to interactive elements.

**Install packages:**

```bash
npm install mouse-follower gsap-trial
```

**Magnetic Cursor Component:**

```tsx
// src/components/ui/MagneticCursor.tsx
'use client'

import { useEffect, useRef } from 'react'
import { gsap } from 'gsap'

export function MagneticCursor() {
  const cursorRef = useRef<HTMLDivElement>(null)
  const followerRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const cursor = cursorRef.current
    const follower = followerRef.current
    if (!cursor || !follower) return

    const moveCursor = (e: MouseEvent) => {
      gsap.to(cursor, {
        x: e.clientX,
        y: e.clientY,
        duration: 0,
      })
      gsap.to(follower, {
        x: e.clientX,
        y: e.clientY,
        duration: 0.3,
      })
    }

    const handleMouseEnter = () => {
      gsap.to([cursor, follower], {
        scale: 1.5,
        duration: 0.3,
      })
    }

    const handleMouseLeave = () => {
      gsap.to([cursor, follower], {
        scale: 1,
        duration: 0.3,
      })
    }

    document.addEventListener('mousemove', moveCursor)
    
    // Add magnetic effect to interactive elements
    const magneticElements = document.querySelectorAll('[data-magnetic]')
    magneticElements.forEach((el) => {
      el.addEventListener('mouseenter', handleMouseEnter)
      el.addEventListener('mouseleave', handleMouseLeave)
    })

    return () => {
      document.removeEventListener('mousemove', moveCursor)
      magneticElements.forEach((el) => {
        el.removeEventListener('mouseenter', handleMouseEnter)
        el.removeEventListener('mouseleave', handleMouseLeave)
      })
    }
  }, [])

  return (
    <>
      <div
        ref={cursorRef}
        className="fixed w-4 h-4 bg-primary rounded-full pointer-events-none z-50 mix-blend-difference"
        style={{ transform: 'translate(-50%, -50%)' }}
      />
      <div
        ref={followerRef}
        className="fixed w-8 h-8 border-2 border-primary/50 rounded-full pointer-events-none z-40"
        style={{ transform: 'translate(-50%, -50%)' }}
      />
    </>
  )
}
```


### 3. **Interactive Terminal Component**

Add a working terminal that showcases your skills.

**Install packages:**

```bash
npm install xterm xterm-for-react @types/xterm react-terminal-ui-kit
```

**Interactive Terminal:**

```tsx
// src/components/interactive/Terminal.tsx
'use client'

import { useState, useEffect } from 'react'
import { motion } from 'framer-motion'

export function InteractiveTerminal() {
  const [input, setInput] = useState('')
  const [history, setHistory] = useState<Array<{type: 'command' | 'output', text: string}>>([
    { type: 'output', text: '🚀 Welcome to Nilesh\'s Interactive Terminal' },
    { type: 'output', text: 'Type "help" to see available commands' }
  ])

  const commands = {
    help: () => [
      'Available commands:',
      '• about - Learn about me',
      '• skills - View my technical skills',  
      '• projects - See my latest projects',
      '• contact - Get my contact info',
      '• clear - Clear terminal'
    ],
    about: () => [
      '👨‍💻 Software Developer & AI/ML Engineer',
      '🎯 3+ years of experience',
      '🌟 50+ projects completed',
      '💡 Passionate about innovative solutions'
    ],
    skills: () => [
      '🔥 Frontend: React, Next.js, TypeScript',
      '⚡ Backend: Node.js, Python, Flask',
      '🤖 AI/ML: TensorFlow, PyTorch',
      '🗄️ Database: PostgreSQL, MongoDB'
    ],
    projects: () => [
      '🎮 ChertNodes - Minecraft hosting platform',
      '🤖 ProtectX - Discord moderation bot',
      '📊 Kahoot Viewer - Quiz answer extraction',
      '🔗 View all projects at /projects'
    ],
    contact: () => [
      '📧 Email: contact@nilesh.dev',
      '💼 GitHub: github.com/nileshkowe',
      '🔗 LinkedIn: Connect with me',
      '💬 Always open for collaborations!'
    ],
    clear: () => { setHistory([]); return [] }
  }

  const executeCommand = (cmd: string) => {
    const command = cmd.toLowerCase().trim()
    const output = commands[command as keyof typeof commands] || (() => [`Command not found: ${cmd}`])
    const result = output()
    
    setHistory(prev => [
      ...prev,
      { type: 'command', text: `$ ${cmd}` },
      ...result.map(text => ({ type: 'output' as const, text }))
    ])
    setInput('')
  }

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="bg-gray-900 border border-gray-700 rounded-lg p-4 font-mono text-sm max-h-96 overflow-y-auto"
    >
      <div className="flex items-center gap-2 mb-4">
        <div className="flex gap-2">
          <div className="w-3 h-3 bg-red-500 rounded-full"></div>
          <div className="w-3 h-3 bg-yellow-500 rounded-full"></div>
          <div className="w-3 h-3 bg-green-500 rounded-full"></div>
        </div>
        <span className="text-gray-400 text-xs ml-2">nilesh@portfolio:~</span>
      </div>
      
      <div className="space-y-1">
        {history.map((item, index) => (
          <div key={index} className={item.type === 'command' ? 'text-primary' : 'text-gray-300'}>
            {item.text}
          </div>
        ))}
      </div>
      
      <div className="flex items-center mt-2">
        <span className="text-primary mr-2">$</span>
        <input
          type="text"
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyPress={(e) => e.key === 'Enter' && executeCommand(input)}
          className="bg-transparent outline-none flex-1 text-white"
          placeholder="Type a command..."
          autoFocus
        />
      </div>
    </motion.div>
  )
}
```


### 4. **Advanced Page Transitions**

Smooth page transitions that wow visitors.

**Install packages:**

```bash
npm install @studio-freight/lenis locomotive-scroll
```

**Page Transition Component:**

```tsx
// src/components/transitions/PageTransition.tsx
'use client'

import { motion, AnimatePresence } from 'framer-motion'
import { usePathname } from 'next/navigation'

const pageVariants = {
  initial: {
    opacity: 0,
    y: 20,
    scale: 0.98
  },
  in: {
    opacity: 1,
    y: 0,
    scale: 1
  },
  out: {
    opacity: 0,
    y: -20,
    scale: 1.02
  }
}

const pageTransition = {
  type: 'tween',
  ease: 'anticipate',
  duration: 0.5
}

export function PageTransition({ children }: { children: React.ReactNode }) {
  const pathname = usePathname()

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={pathname}
        initial="initial"
        animate="in"
        exit="out"
        variants={pageVariants}
        transition={pageTransition}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  )
}
```


### 5. **Real-time Activity Dashboard**

Show live coding activity from GitHub.

**Install packages:**

```bash
npm install swr axios react-activity-calendar
```

**GitHub Activity Component:**

```tsx
// src/components/widgets/GitHubActivity.tsx
'use client'

import useSWR from 'swr'
import { motion } from 'framer-motion'

const fetcher = (url: string) => fetch(url).then(res => res.json())

export function GitHubActivity() {
  const { data, error } = useSWR('/api/github-activity', fetcher, {
    refreshInterval: 30000 // Refresh every 30 seconds
  })

  if (error) return <div>Failed to load activity</div>
  if (!data) return <div>Loading...</div>

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      className="bg-darker/50 backdrop-blur-sm border border-secondary/20 rounded-xl p-6"
    >
      <h3 className="text-white font-semibold mb-4 flex items-center gap-2">
        <div className="w-3 h-3 bg-green-500 rounded-full animate-pulse"></div>
        Live Activity
      </h3>
      <div className="space-y-2 text-sm">
        <div className="flex justify-between text-secondary">
          <span>Commits this week:</span>
          <span className="text-primary">{data.commits || 0}</span>
        </div>
        <div className="flex justify-between text-secondary">
          <span>Lines of code:</span>
          <span className="text-accent">{data.linesOfCode || '10K+'}</span>
        </div>
        <div className="text-xs text-secondary/70">
          Last commit: {data.lastCommit || '2 hours ago'}
        </div>
      </div>
    </motion.div>
  )
}
```


### 6. **Interactive Code Playground**

Let visitors test your code live.

**Install packages:**

```bash
npm install @monaco-editor/react monaco-editor
```

**Code Playground:**

```tsx
// src/components/interactive/CodePlayground.tsx
'use client'

import { Editor } from '@monaco-editor/react'
import { useState } from 'react'
import { motion } from 'framer-motion'

export function CodePlayground() {
  const [code, setCode] = useState(`// Try my code live!
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10));`)

  const [output, setOutput] = useState('')

  const runCode = () => {
    try {
      const result = eval(code)
      setOutput(String(result))
    } catch (error) {
      setOutput(`Error: ${error}`)
    }
  }

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      className="bg-darker/50 backdrop-blur-sm border border-secondary/20 rounded-xl overflow-hidden"
    >
      <div className="flex items-center justify-between p-4 border-b border-secondary/20">
        <h3 className="text-white font-semibold">Code Playground</h3>
        <motion.button
          whileHover={{ scale: 1.05 }}
          whileTap={{ scale: 0.95 }}
          onClick={runCode}
          className="px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary/90 transition-colors"
        >
          Run Code
        </motion.button>
      </div>
      
      <div className="grid grid-cols-1 lg:grid-cols-2">
        <Editor
          height="300px"
          defaultLanguage="javascript"
          value={code}
          onChange={(value) => setCode(value || '')}
          theme="vs-dark"
          options={{
            minimap: { enabled: false },
            fontSize: 14,
          }}
        />
        
        <div className="bg-gray-900 p-4 border-l border-secondary/20">
          <div className="text-sm text-gray-400 mb-2">Output:</div>
          <pre className="text-white text-sm">{output}</pre>
        </div>
      </div>
    </motion.div>
  )
}
```


### 7. **Enhanced Project Showcase with Live Demos**

**Update ProjectsSection.tsx:**

```tsx
// Add to your existing ProjectsSection.tsx
import { Play, Code2, Zap } from 'lucide-react'

// Add this to your project object type
interface Project {
  // ... existing properties
  liveDemo?: string
  techHighlights?: string[]
  performance?: {
    loading: string
    score: number
  }
}

// Enhanced project card component
const EnhancedProjectCard = ({ project }: { project: Project }) => {
  return (
    <motion.div
      variants={itemVariants}
      whileHover={{ y: -10, rotateY: 5 }}
      className="group relative bg-darker/50 backdrop-blur-sm border border-secondary/20 rounded-xl overflow-hidden hover:border-primary/50 transition-all duration-500 transform-style-preserve-3d"
    >
      {/* 3D Hover Effect */}
      <div className="absolute inset-0 bg-gradient-to-r from-primary/10 to-accent/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
      
      {/* Live Demo Button */}
      {project.liveDemo && (
        <motion.button
          whileHover={{ scale: 1.1 }}
          className="absolute top-4 right-4 z-10 p-2 bg-primary/20 backdrop-blur-sm rounded-lg text-white hover:bg-primary/30"
        >
          <Play size={18} />
        </motion.button>
      )}

      {/* Performance Badge */}
      {project.performance && (
        <div className="absolute bottom-4 right-4 px-3 py-1 bg-green-500/20 text-green-400 rounded-full text-xs font-medium">
          <Zap size={12} className="inline mr-1" />
          {project.performance.score}/100
        </div>
      )}

      {/* Rest of your existing project card content */}
    </motion.div>
  )
}
```


### 8. **Dynamic Theme System**

Allow users to customize the experience.

**Install packages:**

```bash
npm install next-themes use-sound
```

**Theme Switcher:**

```tsx
// src/components/ui/ThemeCustomizer.tsx
'use client'

import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { Palette, Sparkles } from 'lucide-react'

const themes = [
  { name: 'Purple Haze', primary: '#C778DD', accent: '#61DAFB' },
  { name: 'Cyber Blue', primary: '#00F5FF', accent: '#FF6B6B' },
  { name: 'Matrix Green', primary: '#00FF00', accent: '#FFD700' },
  { name: 'Sunset Orange', primary: '#FF8C00', accent: '#FF1493' }
]

export function ThemeCustomizer() {
  const [isOpen, setIsOpen] = useState(false)
  const [currentTheme, setCurrentTheme] = useState(0)

  const applyTheme = (theme: typeof themes[^0]) => {
    document.documentElement.style.setProperty('--color-primary', theme.primary)
    document.documentElement.style.setProperty('--color-accent', theme.accent)
  }

  return (
    <div className="fixed right-6 top-1/2 transform -translate-y-1/2 z-50">
      <motion.button
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        onClick={() => setIsOpen(!isOpen)}
        className="p-3 bg-primary/20 backdrop-blur-sm rounded-full text-primary hover:bg-primary/30 transition-colors"
      >
        <Palette size={24} />
      </motion.button>

      <AnimatePresence>
        {isOpen && (
          <motion.div
            initial={{ opacity: 0, x: 100 }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: 100 }}
            className="absolute right-16 top-0 bg-darker/90 backdrop-blur-sm border border-secondary/20 rounded-xl p-4 w-64"
          >
            <h3 className="text-white font-semibold mb-4 flex items-center gap-2">
              <Sparkles size={18} />
              Customize Theme
            </h3>
            <div className="space-y-3">
              {themes.map((theme, index) => (
                <motion.button
                  key={index}
                  whileHover={{ scale: 1.02 }}
                  onClick={() => {
                    setCurrentTheme(index)
                    applyTheme(theme)
                  }}
                  className={`w-full p-3 rounded-lg border transition-all ${
                    currentTheme === index
                      ? 'border-primary bg-primary/10'
                      : 'border-secondary/20 hover:border-primary/50'
                  }`}
                >
                  <div className="flex items-center gap-3">
                    <div className="flex gap-1">
                      <div 
                        className="w-4 h-4 rounded-full" 
                        style={{ backgroundColor: theme.primary }}
                      />
                      <div 
                        className="w-4 h-4 rounded-full"
                        style={{ backgroundColor: theme.accent }}
                      />
                    </div>
                    <span className="text-white text-sm">{theme.name}</span>
                  </div>
                </motion.button>
              ))}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  )
}
```


## 🎯 **Implementation Priority**

1. **Week 1**: 3D Background + Magnetic Cursor + Page Transitions
2. **Week 2**: Interactive Terminal + GitHub Activity + Theme Customizer
3. **Week 3**: Code Playground + Enhanced Project Cards
4. **Week 4**: Performance optimization + final polish

## 📦 **All Packages to Install**

```bash
npm install three @types/three @react-three/fiber @react-three/drei
npm install mouse-follower gsap-trial @studio-freight/lenis
npm install xterm xterm-for-react @types/xterm react-terminal-ui-kit
npm install swr axios react-activity-calendar
npm install @monaco-editor/react monaco-editor
npm install next-themes use-sound
npm install locomotive-scroll leva @react-three/postprocessing three-stdlib
```

These enhancements will transform your portfolio into an **interactive masterpiece** that showcases not just your projects, but your creativity and technical prowess. The combination of 3D graphics, real-time interactions, and dynamic content will definitely make clients go "WOW!" and set you apart from the competition! 🚀

Would you like me to detail any specific implementation or provide more code for particular components?

<div style="text-align: center">⁂</div>

[^1]: AboutMeSection.tsx.txt

[^2]: HeroSection.tsx.txt

[^3]: Footer.tsx.txt

[^4]: Header.tsx.txt

[^5]: globals.css.txt

[^6]: page.tsx.txt

[^7]: ContactSection.tsx.txt

[^8]: QuoteSection.tsx.txt

[^9]: SkillsSection.tsx.txt

[^10]: ProjectsSection.tsx.txt

[^11]: animated-gradient-text.tsx.txt

[^12]: projects_page.tsx.txt

[^13]: contact_page.tsx.txt

[^14]: about_page.tsx.txt

[^15]: particles.tsx.txt

[^16]: dot-pattern.tsx.txt

[^17]: number-ticker.tsx.txt

[^18]: LineDeco.tsx.txt

[^19]: Dots.tsx.txt

[^20]: logger.ts.txt

[^21]: layout.tsx.txt

